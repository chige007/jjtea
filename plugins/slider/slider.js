$.fn.extend({
    slider: function(option){
        console.log(option);
        var defaultOpt = {
            mode: 'horizontal',
            infiniteLoop: true,
            control: true,
            speed: 500,
            cycle: 5000
        }
    }
})